# Port for Azure Container Apps
PORT=3000

# Node environment
NODE_ENV=development

# Include sample icons in the palette (public/icons-sample)
# - true: show icons-sample + icons
# - false: show only icons
ICONS_SAMPLE_ENABLED=true

# Default history retention limit (10-1000)
# NEXT_PUBLIC_HISTORY_LIMIT=100

# Default PNG export scale (1-8)
# NEXT_PUBLIC_EXPORT_SCALE=4

# Simple auth (optional)
# Set both to enable the login screen.
# USER_NAME=admin
# USER_PASS=change-me

# Azure Cosmos DB (optional)
# Set these to enable cloud storage via DefaultAzureCredential.
# COSMOS_ENDPOINT=https://your-account.documents.azure.com:443/
# COSMOS_DATABASE=your-database
# COSMOS_CONTAINER=your-container

# Microsoft Entra ID (MSAL) - optional
# Set these to enable Entra ID authentication.
# When configured, MSAL takes precedence over simple auth.
# Users are isolated by email address in Cosmos DB.
# NEXT_PUBLIC_AZURE_AD_CLIENT_ID=your-app-client-id
# NEXT_PUBLIC_AZURE_AD_TENANT_ID=your-tenant-id
# NEXT_PUBLIC_AZURE_AD_REDIRECT_URI=http://localhost:3000
